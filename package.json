{
  "name": "marriagebureau",
  "version": "1.0.0",
  "private": true,
  "description": "Single Hostinger Node.js entrypoint for Doctor Marriage Bureau frontends with API proxy.",
  "scripts": {
    "user:install": "npm --prefix \"New User Panel Frontend\" ci",
    "mobile:install": "npm --prefix \"DMB Mobile App\" ci",
    "user:ci": "npm run user:install && npm --prefix \"New User Panel Frontend\" run ci",
    "mobile:ci": "npm run mobile:install && npm --prefix \"DMB Mobile App\" run ci",
    "user:build": "npm run user:install && npm --prefix \"New User Panel Frontend\" run build",
    "mobile:build:web": "npm run mobile:install && npm --prefix \"DMB Mobile App\" run build:web",
    "build": "npm run hostinger:build",
    "hostinger:build": "npm run user:build",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
