// LIVE DATA ONLY — all profiles, chats, and metrics are fetched realtime from the
// Go API (/api/v1/discovery, /api/v1/chat, /api/v1/dashboard, etc.). No hardcoded
// demo/default member records remain in this file.

// DEPRECATED: Use live /api/v1/discovery instead. Empty to enforce realtime.
export const MOCK_PROFILES: never[] = [];
export const MOCK_CHATS: never[] = [];
export const COMPATIBILITY_METRICS: never[] = [];
export const RECENT_ACTIVITY: never[] = [];
